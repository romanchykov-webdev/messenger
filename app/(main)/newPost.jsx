import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import ScreenWrapper from "../../components/ScreenWrapper";

const NewPost = () => {
  return (
    <ScreenWrapper >
      <Text>NewPost works!</Text>
    </ScreenWrapper>
  );
};

const styles = StyleSheet.create({})

export default NewPost;
